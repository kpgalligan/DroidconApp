<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:foreground="?attr/selectableItemBackground">

    <co.touchlab.droidconandroid.SquareImageView
        android:id="@+id/item_sponsor_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scaleType="centerCrop"
        android:contentDescription="@string/sponsor_image"
        android:background="@drawable/placeholder_sponsor_image"
    />

    <TextView
        android:id="@+id/debug_item_sponsor_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textSize="30sp"
        tools:text="@string/lorem_name"
    />

</FrameLayout>
